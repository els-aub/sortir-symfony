{% extends 'base.html.twig' %}

{% block title %}Modifier Site{% endblock %}

{% block body %}
    <h1>Modifier Site</h1>

    <div class="form-container">
        {{ include('site/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="actions">
        <a href="{{ path('app_site_index') }}" class="btn">Retour à la liste</a>
    </div>

    {{ include('site/_delete_form.html.twig') }}
{% endblock %}
