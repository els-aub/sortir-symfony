{% extends 'base.html.twig' %}

{% block title %}Site{% endblock %}

{% block body %}
    <h1>Site</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>NoSite</th>
                <td>{{ site.noSite }}</td>
            </tr>
            <tr>
                <th>NomSite</th>
                <td>{{ site.nomSite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_site_index') }}">back to list</a>

    <a href="{{ path('app_site_edit', {'noSite': site.noSite}) }}">edit</a>

    {{ include('site/_delete_form.html.twig') }}
{% endblock %}
