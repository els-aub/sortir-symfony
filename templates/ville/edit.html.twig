{% extends 'base.html.twig' %}

{% block title %}Modifier Ville{% endblock %}

{% block body %}
    <h1>Modifier Ville</h1>

    <div class="form-container">
        {{ include('ville/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>

    <div class="actions">
        <a href="{{ path('app_ville_index') }}" class="btn">Retour à la liste</a>
    </div>

    {{ include('ville/_delete_form.html.twig') }}
{% endblock %}
