<form method="post" action="{{ path('app_sortie_delete', {'idSortie': sortie.idSortie}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sortie.idSortie) }}">
    <button class="btn">Delete</button>
</form>
